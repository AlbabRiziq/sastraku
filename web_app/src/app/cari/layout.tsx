
export const metadata = {
    title: "SASTRAKU | CARI ",
    description: "Cari karya sastra yang kamu sukai disini",
    kewords: "sastra, karya, puisi, cerpen, novel, sastraku, cari"
};

export default function RootLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    );
}
