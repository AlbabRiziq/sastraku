
export const metadata = {
    title: "SASTRAKU | MASUK",
    description: "Mulailah kembali berkarya disini",
    kewords: "sastra, karya, puisi, cerpen, novel, sastraku, cari, login sastraku, login, masuk sastraku, masuk"
};

export default function RootLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    );
}
